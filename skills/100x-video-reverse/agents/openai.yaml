interface:
  display_name: "100X Video Reverse v2.3"
  short_description: "输入视频，联动审阅原片、镜头帧、分段提示词和资产"
  default_prompt: "使用 $100x-video-reverse 反推这条参考视频，并在当前对话中用原片与镜头帧板审阅结果；点击镜头时显示其所属分段提示词。"
policy:
  allow_implicit_invocation: true
